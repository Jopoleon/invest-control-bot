# Итерация 0: подготовка

Дата: 2026-03-07

## Что зафиксировано
- Открытые вопросы из планирования закрыты, кроме условного пункта по импорту (уточняется объем/формат источников).
- Базовая структура репозитория подготовлена.
- Добавлена стратегия конфигурации и секретов через env-переменные.
- Зафиксированы целевые окружения: `local`, `stage`, `prod`.

## Стратегия secrets
- Все секреты передаются только через переменные окружения.
- Секреты не хранятся в git-репозитории.
- Для локальной разработки используется `.env` (игнорируется в `.gitignore`).
- Для `stage/prod` секреты задаются через менеджер окружения VPS (systemd unit env, docker secrets или аналог).

## Обязательные секреты для stage/prod
- `TELEGRAM_BOT_TOKEN`
- `YOOKASSA_SHOP_ID`
- `YOOKASSA_SECRET_KEY`
- `YOOKASSA_WEBHOOK_SECRET`
- `APP_ENCRYPTION_KEY` (минимум 32 символа)
- `ADMIN_AUTH_TOKEN`
- `POSTGRES_DSN`

## Критерии завершения итерации 0
- [x] Подготовлен конфиг-каркас и валидация окружения.
- [x] Подготовлен `.env.example`.
- [x] Зафиксирована структура каталогов проекта.
- [x] Документированы окружения и правила хранения секретов.
